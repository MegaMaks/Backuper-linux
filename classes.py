class Task(object):
  
        def __init__(self,task_name,source,dest,sel_day,task_time,id_server,next_start,time_live,extension,password,exeption):
                self.task_name = task_name
                self.source = source
                self.dest = dest
                self.sel_day = sel_day
                self.task_time = task_time
                self.id_server = id_server
                self.next_start = next_start
                self.time_live = time_live
                self.extension = extension
                self.password = password
                self.exeption = exeption


